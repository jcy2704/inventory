<div class="top">
  <div class="title-cont">
    <h1 class="title"><%= @sale_group.name %> Sales</h1>
  </div>
  <div class="revenue">
    Total Revenue in <%= @sale_group.name%>
    <%= content_tag(:p, number_to_currency(revenue(@sales)), class: "total-rev") %>
  </div>
</div>


<div class="main">
  <div class="edit-group-btn">
    <%= link_to 'Edit Group', edit_sale_group_path, class: 'edit-group' %>
  </div>

  <%= render partial: 'sales/sale_cards' %>
</div>
