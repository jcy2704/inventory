<div class="top">
  <div class="title-cont groups-conf">
    <h1 class="title"><%= @sale_group.name %> Sales<%= link_to icon('fas', 'cog'), edit_sale_group_path, class: 'edit-group' %></h1>
  </div>
  <div class="revenue">
    Total Revenue in <%= @sale_group.name%>
    <%= content_tag(:p, number_to_currency(revenue(@sales)), class: "total-rev") %>
  </div>
</div>


<div class="main">

  <%= render partial: 'sales/sale_cards' %>
</div>
