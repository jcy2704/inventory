<div class="all-users">
  <h1 class="title">Cart</h1>

  <%= link_to "Empty cart", cart_path(@current_cart),method: :delete, data: {confirm: "Are you sure?"} %>
</div>

<ul class="user-cards">
  <% @cart.line_items.each do |u| %>
    <li class="card">
      Item: <%= u.product.name %>/ Price: <%= number_to_currency(u.product.price) %>/ Quantity: <%= u.quantity %>/ Total Price: <%= number_to_currency(u.total_price) %>
        <ul class="flex">
          <li>
            <%= link_to "Add one", line_item_add_path(id: u), method: :post %>
            <%= link_to "Reduce one", line_item_reduce_path(id: u), method: :post %>
          </li>
          <li>
            <%= link_to "Remove item", line_item_path(u), method: :delete %>
          </li>
        </ul>
      </a>
    </li>
  <% end %>
</ul>

<h3>Sub total: <%= number_to_currency(@cart.sub_total) %></h3>

<%= link_to 'Sell', sales_path, method: :post %>
